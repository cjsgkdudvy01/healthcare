<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.2.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
		
	<http pattern="/**/*.js" security="none"/>
	<http pattern="/**/*.css" security="none"/>
	<http auto-config="true">	<!-- use-expressions="true"  true가 default  -->
		<intercept-url pattern="/admin/*" access="hasRole('ROLE_ADMIN')"/>
		<intercept-url pattern="/myPage" access="hasRole('ROLE_USER')"/>	<!-- hasRole('ROLE_ANONYMOUS') or hasRole('ROLE_ADMIN')도 가능 -->
		<intercept-url pattern="/login" access="hasRole('ROLE_ANONYMOUS')"/>
		<intercept-url pattern="/**" access="permitAll"/>

		<form-login
			username-parameter="loginInputId"
			password-parameter="loginInputPassword"
			login-page="/login"
		    login-processing-url="/loginProcess"
			default-target-url="/myPage"
			authentication-failure-url="/login?fail=true"
		/>
		<logout
			logout-url="logoutProcess"
			logout-success-url="/login"
			delete-cookies="JSESSIONID"
		/>
	</http>
	<beans:bean id="CustomJdbcDaoImpl" class="hong.yeongpyo.healthCare.springSecurity.CustomJdbcDaoImpl">
		<beans:property name="dataSource" ref="dataSource"/>
		<beans:property name="rolePrefix" value=""/>
		<beans:property name="usersByUsernameQuery" value="SELECT no, id, password, name, rrn1, rrn2, nick, phonenum1, phonenum2, phonenum3  FROM member WHERE ID = ?"/>
		<beans:property name="authoritiesByUsernameQuery" value="SELECT code FROM authority where no in (SELECT auth_no FROM member_auth WHERE member_no=?)"/>
		<beans:property name="enableGroups" value="false"/>
	</beans:bean>
	<authentication-manager>
			<authentication-provider user-service-ref="CustomJdbcDaoImpl"/>
	</authentication-manager>
	<!-- 오류메세지 -->
	<beans:bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
		<beans:property name="basenames">
			<beans:list>
				<beans:value>/WEB-INF/properties/spring_security_message</beans:value>
			</beans:list>
		</beans:property>
		<beans:property name="defaultEncoding" value="UTF-8"/>
		<beans:property name="cacheSeconds" value="5"/>
	</beans:bean>
	
</beans:beans>
